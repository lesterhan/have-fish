{
  "name": "have-fish",
  "version": "0.1.0",
  "private": true,
  "description": "Root workspace: holds the formatter and linter that span backend, frontend, mobile and ops.",
  "scripts": {
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "format": "biome format --write .",
    "lint": "biome lint ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.14"
  }
}
