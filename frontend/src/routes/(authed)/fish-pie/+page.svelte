<script lang="ts">
</script>

<div class="page">
  <div class="window">
    <div class="titlebar">
      <span class="title">Fish Pie — Expense Splitting</span>
    </div>
    <div class="body">
      <p class="wip">Coming soon.</p>
    </div>
  </div>
</div>

<style>
  .page {
    padding: var(--sp-lg);
  }

  .window {
    background: var(--color-window);
    box-shadow: var(--shadow-raised);
    max-width: 480px;
  }

  .titlebar {
    background: linear-gradient(to right, var(--color-titlebar-from), var(--color-titlebar-to));
    padding: 4px var(--sp-sm);
    font-size: var(--text-sm);
    font-weight: var(--weight-semibold);
    color: #fff;
  }

  .body {
    padding: var(--sp-lg);
  }

  .wip {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    margin: 0;
  }
</style>
